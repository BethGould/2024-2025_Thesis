This code builds a grid of solutions to the initial value problem, in order to calculate t_slow (= 2*pi / M), 
the period of the slow oscillations. Here we can vary R, B, k, dk, mu, A, and Psi'(0) and 
construct either a grid of values, or generate a set of random values. 

The Psi'(0) grid is spaced sinusoidally. The mu grid is spaced logarithmically. The other grids 
are spaced linearly. Note that one of the results of this is that mu is taken as a exponent of 
powers of 10 for the grid, while it is taken as a raw number for Monte Carlo runs.

grid_slow_osc is built off of grid_fast_osc, inheriting the routines for forming the grid, 
as the structure is identical. 

Этот код строит сетку решений задачи о начальном значении, чтобы вычислить t_slow (= 2*pi /M),
период медленных колебаний. Здесь мы можем варьировать значения R, B, k, dk, mu, A и Psi'(0) и
построить либо сетку значений, либо сгенерировать набор случайных значений. 

Сетка Psi'(0) расположена синусоидально. Сетка mu разнесена логарифмически. Остальные сетки
разнесены линейно. Обратите внимание, что одним из результатов этого является то, что mu берется как показатель
степени 10 для сетки, в то время как для прогонов по методу Монте-Карло оно берется как исходное число.

grid_slow_osc построен на основе grid_fast_osc, наследуя процедуры формирования сетки,
поскольку структура идентична.

called by init

need to run init -> makeGridPoints -> gridSlowOsc

Grid run code:
Monte Carlo run code:

  Creates a loop object with the given parameters. Note that the passed values of parameters
  are relevant only for those parameters which are not to be varied as varied parameters will 
  be replaced during the calculations.

  Prints a description of the object. 

вызывается с помощью init

необходимо запустить init -> makeGridPoints -> gridSlowOsc

Код запуска сетки:
Код запуска по методу Монте-Карло:

  Создает объект цикла с заданными параметрами. Обратите внимание, что переданные значения параметров
актуальны только для тех параметров, которые не подлежат изменению, поскольку измененные параметры будут
заменены в процессе вычислений.

  Выводит описание объекта.

save_solution -- Boolean set to false by default. If true, the full solutions to the initial value problem will be saved. 
 This is used as the solutions can be large, and are rarely needed.

clear_calcs(self) -- Clears the data. 
The default parameter values are: solve_mu_0 = False, n_sm = 20, method = 'RK45', rtol = rtol, atol = atol, R_max = 1.0, n_lg = 1000.
n_sm counts how many oscillations of |Psi|^2 to count in order to estimate t / 2, (which is pi / k).
n_lg counts how many points to sample of Psi when the fast oscillations reaches its maximum value. This will calculate the curve 
for the slow oscillations. 
If solve_mu_0 = True, the code will estimate the fast oscillations for the case without ee interaction as well.
Parameters not given will not be changed.

save_solution - Логическое значение по умолчанию равно false. Если значение true, будут сохранены полные решения задачи с исходным значением. 
 Это используется, поскольку решения могут быть большими и редко требуются.

clear_calcs(self) - Очищает данные. 
Значения параметров по умолчанию следующие: solve_mu_0 = False, n_sm = 20, method = 'RK45', rtol = rtol, atol = atol, R_max = 1.0, n_lg = 1000.
n_sm подсчитывает, сколько колебаний |Psi| ^ 2 необходимо подсчитать, чтобы оценить t / 2 (что равно pi / k).
n_lg подсчитывает, сколько точек будет выбрано для измерения Psi, когда быстрые колебания достигнут своего максимального значения. Это позволит рассчитать кривую 
для медленных колебаний. 
Если solve_mu_0 = True, код также оценит быстрые колебания для случая без взаимодействия с ee.
Не заданные параметры изменены не будут.

These define the various values of parameters for which to analyze the initial value problem. If a set of points is not included,
it will not be varied, instead using the value of the parameter passed in initialization. Otherwise, the values
will be varied from the minimum value given to the maximum value given. 
  !!! Note -- mu is varied based on exponents of 10 for the grid points, but as a pure number for Monte Carlo 
  analysis. I know this is an issue, but it how things are written.
  num is a positive integer indicating the number of points for the Monte Carlo case.
  For a grid, num must be a list of positive integers. A list size of one will set all dimensions of the grid to
this number, for num ^ n points. You can send two numbers to provide separate lengths for derivatives and other
numbers, or more to provide a value for every parameter separately. (The real and imaginary derivatives must be 
equal, and they will never take two parameters, only one.)

Они определяют различные значения параметров, для которых необходимо проанализировать проблему с исходным значением. Если набор точек не включен,
он не будет изменен, вместо этого будет использоваться значение параметра, переданного при инициализации. В противном случае значения
будут варьироваться от заданного минимального значения до заданного максимального значения. 
  !!! Примечание - mu изменяется на основе показателей, равных 10 для точек сетки, но как чистое число для
анализа методом Монте-Карло. Я знаю, что это проблема, но так все записано.
  num - положительное целое число, указывающее количество баллов для случая Монте-Карло.
  Для сетки num должно быть списком натуральных чисел. При размере списка, равном единице, все измерения сетки
будут равны этому числу для num ^ n точек. Вы можете отправить два числа, чтобы указать отдельные длины для производных и других
чисел, или несколько, чтобы указать значение для каждого параметра отдельно. (Действительная и мнимая производные должны быть
равны, и они никогда не будут принимать два параметра, только один.)

gridSlowOsc(self) does the work of the calculation if given a grid, with all of the time spent here.
mcSlowOsc(self) does the work of the calculation if given a random assortment of points to run.
runCalc(self) does the work of the calculation for either case.

gridSlowOsc(self) выполняет вычислительную работу, если задана сетка, с учетом всего затраченного на это времени.
mcSlowOsc(self) выполняет вычислительную работу, если задан случайный набор точек для запуска.
runCalc(self) выполняет работу по вычислению в любом случае.

Results are saved in the following variables:
self.slow_osc_k   -- Estimate for our wavenumber for the slow oscillations. (Found from fitting to a sine function.)
self.slow_osc_a   -- Estimate our slow oscillation amplitude.
self.slow_osc_th  -- Estimate our slow oscillation angle offset from a sine function.
self.slow_osc_sol -- Saves our full solution. Note that this is saved as a list rather than an array like the other solutions.
self.slow_osc_i   -- The i-th element of this list matches the full solution to the array indicies of the other estimates, 
 as given by the contained list.
 For i[j] = [n_mu, n_dk, n_b, n_r, n_a, n_k0, n_d, n_d] (where j and all ns are integers),
 sol[j] is the full solution for the estimated k[i], a[i], and th[i].
 This last variable is used only for the grid solution, as all variables have one index for the Monte 
 Carlo case.

Результаты сохраняются в следующих переменных:
self.slow_osc_k - Оценка нашего волнового числа для медленных колебаний. (Определяется путем подгонки к синусоидальной функции.)
self.slow_osc_a - Оценка амплитуды наших медленных колебаний.
self.slow_osc_th - Вычисляет смещение угла нашего медленного колебания от синусоидальной функции.
self.slow_osc_sol - Сохраняет наше полное решение. Обратите внимание, что оно сохраняется в виде списка, а не массива, как другие решения.
self.slow_osc_i - i-й элемент этого списка соответствует полному решению по показателям массива других оценок,
как указано в содержащемся списке.
 Для i[j] = [n_mu, n_dk, n_b, n_r, n_a, n_k0, n_d, n_d] (где j и все n являются целыми числами),
sol[j] является полным решением для расчетных значений k[i], a[i] и th[i].
 Эта последняя переменная используется только для сеточного решения, так как все переменные имеют один индекс для монте-карло. 
 Дело Карло.

Parameters are saved in the following variables.
- Monte Carlo Table:
self.val_table-- The table of random values of parameters for the Monte Carlo variant.
- Grid Tables:
magnetic_field_strength  = self.mfs
electron_wavenumber  = self.ew
electron_wavenumber_diff = self.ewd
nonlinearity_strength= self.nls
starting_amplitude   = self.amp
ring_radius  = self.rr
d0_grid[idr,idi]
starting_derivative_real = self.dr
starting_derivative_imag = self.di

Параметры сохраняются в следующих переменных.
- Таблица Монте-Карло:
self.val_table - Таблица случайных значений параметров для варианта Монте-Карло.
- Таблицы сетки:
magnetic_field_strength = self.mfs
электронный волновой номер = self.ew
электронный волновой номер_diff = self.ew
нелинейность = self.nls
начальная амплитуда = self.amp
кольцевое излучение = self.rr
d0_grid[idr,idi]
начальный производный_real = self.dr
начальный производный_imag = self.di

------ INITIALIZATION --------------

I have parameters here set to remove a call to the integrator, hopefully saving some time.

self.save_solution = False Tests with the solution saved show that it is too large.

Because we want to know what our object is.
str = "Grid object to measure slow oscillations:\n"
str = "Uncalculated grid object to measure slow oscillations:\n"

------ ИНИЦИАЛИЗАЦИЯ --------------

Здесь у меня установлены параметры, позволяющие отменить вызов интегратора, что, надеюсь, сэкономит немного времени.

self.save_solution = False Тесты с сохраненным решением показывают, что оно слишком большое.

Потому что мы хотим знать, что представляет собой наш объект.
str = "Сеточный объект для измерения медленных колебаний:\n"
str = "Нерасчетный сеточный объект для измерения медленных колебаний:\n"

Indicate our parameters for our grid.
str = f"Monte Carlo object to measure slow oscillations:\n"
str = f"Uncalculated Monte Carlo object to measure slow oscillations:\n"
The following output is not eligant.
str = str + f"mu has points from {np.min(self.val_table[:,0])} to {np.max(self.val_table[:,0])}.\n"
str = str + f"dk has points from {np.min(self.val_table[:,1])} to {np.max(self.val_table[:,1])}.\n"
str = str + f"B has points from {np.min(self.val_table[:,2])} to {np.max(self.val_table[:,2])}.\n"
str = str + f"R has points from {np.min(self.val_table[:,3])} to {np.max(self.val_table[:,3])}.\n"
str = str + f"A has points from {np.min(self.val_table[:,4])} to {np.max(self.val_table[:,4])}.\n"
str = str + f"k0 has points from {np.min(self.val_table[:,5])} to {np.max(self.val_table[:,5])}.\n"
str = str + f"Number of points: {self.val_table.shape[0]}"
return "Empty object to measure slow oscillations:\nR is %s, B is %s, dk is %s, k is %s, mu is %s, A is %s" % (self.R, self.B, self.dk, self.k, self.mu, self.amp)

Укажите наши параметры для нашей сетки.
str = f"Объект Монте-Карло для измерения медленных колебаний:\n"
str = f"Нерасчетный объект Монте-Карло для измерения медленных колебаний:\n"
Следующий результат не подходит.
str = str + f"у mu есть точки от {np.min(self.val_table[:,0])} до {np.max(self.val_table[:,0])}.\n"
str = str + f"у dk есть точки от {np.min(self.val_table[:,1])} до {np.max(self.val_table[:,1])}.\n"
str = str + f"B имеет значения от {np.min(self.val_table[:,2])} до {np.max(self.val_table[:,2])}.\n"
str = str + f"R содержит точки от {np.min(self.val_table[:,3])} до {np.max(self.val_table[:,3])}.\n"
str = str + f"A содержит точки от {np.min(self.val_table[:,4])} до {np.max(self.val_table[:,4])}.\n"
str = str + f"k0 содержит точки от {np.min(self.val_table[:,5])} до {np.max(self.val_table[:,5])}.\n"
str = str + f"Количество точек: {self.val_table.shape[0]}"
возвращает "Пустой объект для измерения медленных колебаний:\nR - это %s, B - это %s, dk - это %s, k - это %s, mu - это %s, A - это %s" % (self.R, self.B, self.dk, self.k, self.му, я сам.усилитель)

------ MODIFIED FUNCTIONS -------

This is changed, because the data is saved in a different manner.

R_max is not relevant for the fast oscillations, as only the first n fast oscillations are calculated for this 
estimate. So this works as the previous case, but now with an R_max parameter, to allow overcalculating, for 
better M estimates.

------ ИЗМЕНЕННЫЕ ФУНКЦИИ -------

Это изменено, поскольку данные сохраняются другим способом.

Значение R_max не относится к быстрым колебаниям, поскольку для этой оценки вычисляются только первые n быстрых колебаний
. Таким образом, это работает так же, как и в предыдущем случае, но теперь с параметром R_max, позволяющим выполнить перерасчет для
получения более точных оценок M.

------------- FIND VALUES ON GRID ---------------

Wrapper for the two following functions to run the code.
print("Points to calculate have not yet been set.")

Remove the inherited function.
print("Incorrect type of object. Try gridSlowOsc instead.")

Perform the calculation on the grid.
This take time, but no longer requires parameters. It is separate from the parameters due to this requirement.

If old calculation already exists.

If the grid is not set.
raise Exception("gridSlowOsc requires forming the grid first. Try calling makeGridPoints.") 

------------- НАЙДИТЕ ЗНАЧЕНИЯ В ТАБЛИЦЕ ---------------

Для запуска кода используйте оболочку для двух следующих функций.
print("Точки для расчета еще не заданы")

Удалите унаследованную функцию.
print("Неверный тип объекта. Попробуйте вместо этого gridSlowOsc.")

Выполните вычисление по сетке.
Это требует времени, но больше не требует параметров. Из-за этого требования оно не связано с параметрами.

Если старое вычисление уже существует.

Если сетка не задана.
вызовите исключение("gridSlowOsc требует сначала формирования сетки. Попробуйте вызвать makeGridPoints".)

Integration parameters.
pr = self.R_max can be changed if more cycles needed
plot_code = 1   only plot er, saving time

Timing
print('Begin grid build: ', time.time() - start_time)

Initialize grids

saves solution for analysis

saved data

slow_osc_sv_ind  = [] indicies to match full solution to other saved data
slow_osc_sv_data = [] full solution

Indicies for each parameter to vary.

Параметры интеграции.
pr = self.R_max может быть изменен, если требуется больше циклов
, plot_code = 1 только для построения графика, что экономит время

Печать по времени ('Начать построение сетки: ', time.time() - start_time)

Инициализация сетки

сохраняет решение для анализа

сохраненные данные

slow_osc_sv_ind = [] значения для сопоставления полного решения с другими сохраненными данными
slow_osc_sv_data = [] полное решение

Значения для каждого параметра могут отличаться.

Solve for each point on the grid.
The 8 levels are only a problem if they are all used.
print("Number of periods to calculate:", num_dk*num_k0*num_b*num_r*num_mu*num_a*num_d*num_d)
print(f"mu: {im}, dk: {ik}, Time: {time.time() - start_time}")
And for the derivative.
Calculate the derivative from our parameters, if it is not as given.
Estimate k. This is no longer automatic, and it is required. 

Retrieve our solution
Fit to a sinusoid.

Save our full data as well.

Решите для каждой точки на сетке.
8 уровней являются проблемой только в том случае, если все они используются.
вывести("Количество периодов для расчета:", num_dk*num_k0*num_b*num_r*num_mu*num_a*num_d*num_d)
выведите(f"mu: {im}, dk: {ik}, Time: {time.time() - start_time}")
И для производной.
Вычислите производную по нашим параметрам, если она не соответствует заданным.
Оцените k. Это больше не выполняется автоматически, и это требуется. 

Извлеките наше решение
Расположите его по синусоиде.

Сохраните также наши полные данные.

Transfer our local variables to object variables, saving our data.
self.slow_osc_k  = slow_oscillation_wavenumber  Estimate for our wavenumber for the slow oscillations (found from fitting a sin).
self.slow_osc_a  = slow_oscillation_amplitude   Maximum absolute value.
self.slow_osc_i  = slow_osc_sv_ind  Indicies for our spacings, due to the alternative storage mechanism.
self.slow_osc_sol  = slow_osc_sv_data   Save full solution.

Indicate that this function has been run.

And end timing.
print("Done grid build: ", time.time() - start_time)

Переведем наши локальные переменные в объектные, сохранив наши данные.
self.slow_osc_k = оценка волнового числа slow_oscillation_wavenumber для нашего волнового числа для медленных колебаний (найдено путем подгонки sin).
self.slow_osc_a = максимальное абсолютное значение амплитуды медленных колебаний.
self.slow_osc_i = slow_osc_sv_ind Указывает на наши интервалы, благодаря альтернативному механизму хранения.
self.slow_osc_sol = slow_osc_sv_data Сохранить полное решение.

Указать, что эта функция была запущена.

И время окончания.
напечатать("Построение сетки завершено", time.time() - время начала)

Delete the inherited function.
print("Wrong type of object. Try mcSlowOsc.")

Now our run for randomly determined points.

Old calculation already exists.

The grid is not set.
raise Exception("mcSlowOsc requires choosing the points first. Try calling makeMCPoints.") 

Integration parameters.
pr = self.R_max can be changed if more cycles needed
plot_code = 1   only plot er, saving time

Удалите унаследованную функцию.
напечатайте("Неверный тип объекта. Попробуйте mcSlowOsc.")

Теперь мы запускаем для случайно определенных точек.

Старый расчет уже существует.

Сетка не задана.
вызовите исключение("mcSlowOsc требует сначала выбрать точки. Попробуйте вызвать makeMCPoints.") 

Параметры интеграции.
pr = self.R_max можно изменить, если требуется больше циклов
, plot_code = 1 только для построения графика, что экономит время

Timing
print('Begin grid build: ', time.time() - start_time)

Retrieve number of points.

Saves solution for analysis.

Solve for each rndomly determined point.
print("Number of periods to calculate:", num)
Calculate derivative from our parameters if it is not as given.
Estimate k. This is no longer automatic, and it is required.

Временная
печать("Начать построение сетки: ", time.time() - start_time)

Извлекает количество точек.

Сохраняет решение для анализа.

Решает для каждой случайно выбранной точки.
печатает("Количество периодов для расчета:", num)
Вычислите производную от наших параметров, если она не соответствует заданной.
Оцените k. Это больше не выполняется автоматически, и это требуется.

Retrieve our solution.

Fit to a sinusoidal function.

And also save our full solution.

Transfer our local variables to object variables, saving our data.
self.slow_osc_k  = slow_oscillation_wavenumber  Estimate for our wavenumber for the slow oscillations (found from fitting a sin).
self.slow_osc_a  = slow_oscillation_amplitude   Maximum absolute value.
if self.save_solution: self.slow_osc_sol  = sol_er_u_save  Save full solution.

Indicate that this function has been run.

And end timing.
print("Done grid build: ", time.time() - start_time)

Извлеките наше решение.

Приведите его к синусоидальной функции.

А также сохраните наше полное решение.

Переведите наши локальные переменные в объектные переменные, сохранив наши данные.
self.slow_osc_k = оценка волнового числа медленных колебаний для нашего волнового числа для медленных колебаний (найденного путем подгонки sin).
self.slow_osc_a = максимальное абсолютное значение амплитуды медленных колебаний.
если self.save_solution: self.slow_osc_sol = sol_er_u_save Сохранит полное решение.

Укажите, что эта функция была запущена.

И завершите отсчет времени.
print("Построение сетки завершено: ", time.time() - start_time)