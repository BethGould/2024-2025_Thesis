-- dk, R2, B2, and mu are our loop parameters, as we pass to create a loop.
-- R2 is our R parameter and B2 is our B parameter.
-- Note that R2 and B2 here are percents, as is effectively dk, like those used to create a loop object.

-- dk, R2, B2 и mu - это наши параметры цикла, которые мы передаем для создания цикла.
-- R2 - это наш параметр R, а B2 - это наш параметр B.
-- Обратите внимание, что R2 и B2 здесь - это проценты, что фактически равно dk, как и те, которые используются для создания объекта цикла.

This is my full script for creating graphs of the solution to the BVP. The shown graphs will be for
the real part of the wavefunction, where it follows maxima of the absolute value of the fast oscillations.
Therefore, the plotted function will be for the slow oscillation graph. There are two possible graphical 
solutions to the linear case and four for the nonlinear case. For both cases, 
 - Psi_slow(0) = Psi_slow(2*pi*R) or
 - Psi_slow(0) = - Psi_slow(2*pi*R).
The second case occurs if the matching of the fast oscillation switches sign, since this matching only 
requires equal amplitude. 
For the linear case, the derivatives of the absolute value at the beginning and end must have opposite signs.
For the nonlinear case, the derivative may also match for the final solution, since dk is not selected to 
prevent this case, as it doesn't create as severe an ambiguity in the matching solution. 

Это мой полный скрипт для создания графиков решения BVP. Показанные графики будут для
действительной части волновой функции, где она следует за максимумами абсолютного значения быстрых колебаний.
Следовательно, построенная функция будет отображаться на графике медленных колебаний. Существует два возможных графических
решения для линейного случая и четыре для нелинейного. В обоих случаях
- Psi_slow(0) = Psi_slow(2*pi*R) или
 - Psi_slow(0) = - Psi_slow(2*pi*R).
Второй случай возникает, если согласование быстрых колебаний меняет знак, поскольку для этого согласования
требуется только равная амплитуда. 
Для линейного случая производные от абсолютного значения в начале и в конце должны иметь противоположные знаки.
Для нелинейного случая производная также может совпадать с конечным решением, поскольку dk не выбран для
предотвращения этого случая, поскольку это не создает столь серьезной двусмысленности в соответствующем решении.

Outputs will be displayed on the screen.

This is a script to match the nonlinear solution at the boundary using the model for k and M
and output results on the screen. 
The root of our matching equation found is effectively random (chaotic, not random). There are many. 
The program starts looking for roots around the linear solution, but depending on the algorithm, the solution may 
not be the closest one.

Find the roots
print("Root of linear solution:", deriv_psi_o) 
print("Value of matching function at this root (should be 0):", xs_sol)
print("Root of nonlinear solution:", deriv_psi)

Выходные данные будут отображены на экране.

Это сценарий для сопоставления нелинейного решения на границе с использованием модели для k и M
и вывода результатов на экран. 
Корень найденного нами уравнения сопоставления является фактически случайным (хаотичным, а не случайноположительным). Есть много. 
Программа начнет поиск корней вокруг линейного решения, но, в зависимости от алгоритма, решение может
оказаться не самым близким.

Найдите корни
print("Корень линейного решения:", deriv_psi_o). 
print("Значение соответствующей функции в этом корне (должно быть равно 0):", xs_sol)
print("Корень нелинейного решения:", deriv_psi)

Our outputs separate the real and imaginary parts of our psi_0 derivative as if they were separate variables. We need to reunite them as a single complex number.

Define a loop object for use with this root

new line

a, b for calculated and fit linear solutions
They are typically very close.

print("aj, bj for the calculated linear solution: ", l_calc.aj, l_calc.bj)
print("aj, bj for the fit linear solution: ", l_calc.aj, l_calc.bj)

Наши выходные данные разделяют действительную и мнимую части нашей производной psi_0, как если бы они были отдельными переменными. Нам нужно объединить их в одно комплексное число.

Определите объект цикла для использования с этим корнем

новая строка

a, b для вычисляемых и подходящих линейных решений
Обычно они очень близки.

print("aj, bj для вычисленного линейного решения: ", l_calc.aj, l_calc.bj)
print("aj, bj для подходящего линейного решения: ", l_calc.aj, l_calc.bj)

Now for the nonlinear bvp solution.

Check to see how good the match is, here using the model for k and M, not the ode solver.
print("Exact solution endpoint:", l_calc.psij0(2*pi*R2 * R_max))
print("New solution endpoint:", l_calc.psij_pred(2*pi*R2 * R_max), "Absolute value =", np.abs(l_calc.psij_pred(2*pi*R2 * R_max)))

linear and nonlinear coefficients 
print("aj0, bj0 (linear solution):", l_calc.aj0, l_calc.bj0)
current calculations

Теперь перейдем к нелинейному решению bvp.

Проверьте, насколько хорошо согласуется результат, используя модель для k и M, а не ode-решатель.
вывести("Конечная точка точного решения:", l_calc.psij0(2*pi*R2 * R_max))
вывести("Конечная точка нового решения:", l_calc.psij_pred(2*pi*R2 * R_max), "Абсолютное значение =", np.abs(l_calc.psij_pred(2*pi*R2 * R_max)))

вывод линейных и нелинейных коэффициентов
("aj0, bj0 (линейное решение):", l_calc.aj0, l_calc.bj0)
текущие вычисления

print("Nonlinear current estimate:", cur_new)
print("Linear current estimate:", cur_old)

Now all outputs are graphical rather than text based.

ODE solver with our solutions, for visual analysis of our matching

All this code below is a copy of that from our derivative grid plotting script.
We are retrieving our x and Psi data from our loop for everything we wish to plot.

print("Нелинейная текущая оценка:", cur_new)
print("Линейная текущая оценка:", cur_old)

Теперь все выходные данные являются графическими, а не текстовыми.

ODE solver с нашими решениями для визуального анализа соответствия

Весь приведенный ниже код является копией кода из нашего сценария построения производной сетки.
Мы извлекаем наши данные x и Psi из нашего цикла для всего, что мы хотим построить.

Initial parameters

fast, slow, pos, neg

This is our exact solution, following the fast oscillations.
Find our modelled wavenumber.
Fit all parameters except the wavenumber to a sine.

position arrays
estimated, triggered by abs, triggered by real (both) -- c, a, r
value arrays
Clear imaginary part

Начальные параметры

быстрое, медленное, положительное, отрицательное

Это наше точное решение, основанное на быстрых колебаниях.
Найдите наше смоделированное волновое число.
Приведите все параметры, кроме волнового числа, к синусу.

массивы
значений, рассчитанные с помощью abs, срабатывающие с помощью real (оба) -- массивы значений c, a, r

Очистить мнимую часть

First plot

plt.title(f"Re(\u03A8(x)), for maxima of |\u03A8(x)|, dk={dk}, R={R2}, B={B2}, \u03BC={mu}")

Second plot of only the predicted value.

plt.title(f"Plot of modelled slow oscillations of Re(\u03A8(x)), dk={dk}, R={R2}, B={B2}, \u03BC={mu}")

Первый сюжет

plt.заголовок(f"Re(\u03A8(x)), для максимумов |\u03A8(x)|, dk={dk}, R={R2}, B={B2}, \u03BC={mu}")

Второй график показывает только прогнозируемое значение.

plt.название(f"График смоделированных медленных колебаний Re(\u03A8(x)), dk={dk}, R={R2}, B={B2}, \u03BC={mu}")

Third plot
real triggered envelope

plt.title(f"Re(\u03A8(x)) envelope, dk={dk}, R={R2}, B={B2}, \u03BC={mu}")
ax.set_ylabel('Re(\u03A8) (ratio of \u03A8(0))')
ax.set_xlabel('x (m)')

line1, = ax.plot(tuerr, suerr, color = 'red', label = 'with e-e interaction')
line13, = ax.plot(t_pred, y_pred, color = 'goldenrod', label = 'predicted e-e interaction')
line9, = ax.plot(tl0xc, sl0xc, color = 'purple', label = 'exact solution')

Третий участок
с реальным срабатыванием конверта

plt.title(огибающая f"Re(\u03A8(x)), dk={dk}, R={R2}, B={B2}, \u03BC={mu}")
ax.set_ylabel('Re(\u03A8) (соотношение \u03A8(0)))')
ax.set_xlabel('x (m)')

строка 1, = ax.plot(tuerr, suerr, color = "красный", label = "с электронным взаимодействием")
строка 13, = ax.plot(t_pred, y_pred, color = "золотарник", label = "прогнозируемое электронное взаимодействие")
строка 9, = ax.plot(tl0xc, sl0xc, color = "фиолетовый", label = "точное решение")

This is an old, unused version of the above script.

Это старая, неиспользуемая версия вышеупомянутого скрипта.

set parameters

print("Root of linear solution:", xs, yy(xs))
print("Root of nonlinear solution:", yy(dphi0c))
print("Exact solution endpoint:", l_calc.psij0(2*pi*R2 * R_max))
print("New solution endpoint:", l_calc.psij_pred(2*pi*R2 * R_max), np.abs(l_calc.psij_pred(2*pi*R2 * R_max)))
print("New and old current estimates:", cur_new, cur_old)

position arrays
estimated, triggered buy abs, triggered by real (both) -- c, a, r

value arrays
real triggered envelope

установите параметры

print("Корень линейного решения:", xs, yy(xs))
print("Корень нелинейного решения:", yy(dphi0c))
вывести("Конечная точка точного решения:", l_calc.psij0(2*pi*R2 * R_max))
вывести("Конечная точка нового решения:", l_calc.psij_pred(2*pi*R2 * R_max), np.abs(l_calc.psij_pred(2*pi*R2 * R_max)))
print("Новые и старые текущие оценки:", cur_new, cur_old)

массивы позиций
оценены, активированы на покупку abs, активированы на реальные (оба) -- c, a, r

массивы значений
конверт, активированный на реальные значения